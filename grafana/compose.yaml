services:
  grafana:
    image: docker.io/grafana/grafana-oss:12.3.2
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana

volumes:
  grafana_data:
    driver: local
